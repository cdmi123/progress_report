<h3 class="text-primary">Welcome, <%= student.name %></h3>
<p><strong>Registered Courses:</strong>
  <% student.courses.forEach(c => { %>
    <span class="badge bg-secondary"><%= c.name %></span>
  <% }) %>
</p>

<div class="card mt-4 shadow-sm">
  <div class="card-body">
    <h5 class="mb-3">📋 Your Progress Reports</h5>
    <ul class="list-group">
      <% reports.forEach(r => { %>
        <li class="list-group-item d-flex justify-content-between align-items-center">
          <strong><%= r.course.name %></strong>
          <a href="/student/report/<%= r._id %>" class="btn btn-sm btn-primary">View Report</a>
        </li>
      <% }) %>
    </ul>
  </div>
</div>
